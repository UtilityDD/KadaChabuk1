<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Animator for the entering activity (the main content) -->
    <objectAnimator
        android:valueFrom="90"
        android_valueTo="0"
        android:propertyName="rotationY"
        android:interpolator="@android:interpolator/accelerate_decelerate"
        android:duration="500" />

    <!-- Fade in the main content as it flips -->
    <objectAnimator android:valueFrom="0.0" android:valueTo="1.0" android:propertyName="alpha" android:duration="500" />
</set>